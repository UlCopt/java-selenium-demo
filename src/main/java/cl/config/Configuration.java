package cl.config;

import java.time.Duration;

public class Configuration {

    final public static Duration TIMEOUT_DURATION = Duration.ofSeconds(30);
    final public static Duration POLLING_DURATION = Duration.ofSeconds(5);
}
